version: 2
updates:
  # Dependabot uses the "npm" ecosystem for Node.js projects,
  # including repositories managed with pnpm lockfiles.
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    target-branch: "main"
    cooldown:
      default-days: 7
